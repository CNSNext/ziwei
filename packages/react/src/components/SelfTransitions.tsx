import type { SelfTransformation, StarProps } from "@ziweijs/core";

export interface SelfTransitionsProps {
  x: number;
  y: number;
  type: SelfTransformation;
  ST: StarProps["ST"];
}

export default function SelfTransitions() {
  //
}
