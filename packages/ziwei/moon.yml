layer: "library"
language: "typescript"

tasks:
  dev:
    command: "rslib build --watch"
    inputs:
      - "src/**/*"
      - "rslib.config.ts"
      - "tsconfig.json"
    outputs:
      - "dist"
    local: true
  build:
    command: "rslib build"
    inputs:
      - "src/**/*"
      - "rslib.config.ts"
      - "tsconfig.json"
    outputs:
      - "dist"
